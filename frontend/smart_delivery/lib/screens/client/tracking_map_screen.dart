import 'package:flutter/material.dart';

class TrackingMapScreen extends StatelessWidget {
  const TrackingMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Carte de suivi'));
  }
}
